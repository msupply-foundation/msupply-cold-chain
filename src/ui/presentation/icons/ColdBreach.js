import FontAwesome from 'react-native-vector-icons/FontAwesome';
import { STYLE, COLOUR, ICON } from '~constants';

export const ColdBreach = () => {
  return <FontAwesome size={STYLE.ICON.SIZE.L} name={ICON.COLD_BREACH} color={COLOUR.WHITE} />;
};
