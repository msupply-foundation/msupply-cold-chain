import FontAwesome from 'react-native-vector-icons/FontAwesome';
import { STYLE, COLOUR, ICON } from '~constants';

export const HotBreach = () => {
  return <FontAwesome size={STYLE.ICON.SIZE.L} name={ICON.HOT_BREACH} color={COLOUR.WHITE} />;
};
