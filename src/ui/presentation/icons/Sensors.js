import FontAwesome from 'react-native-vector-icons/FontAwesome';
import { ICON, COLOUR, STYLE } from '~constants';

export const Sensors = () => {
  return <FontAwesome name={ICON.SENSORS} size={STYLE.ICON.SIZE.S} color={COLOUR.OFF_WHITE} />;
};
