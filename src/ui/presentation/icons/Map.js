import FontAwesome from 'react-native-vector-icons/FontAwesome';
import { COLOUR, STYLE } from '~constants';

export const Map = () => {
  return <FontAwesome name="map" size={STYLE.ICON.SIZE.S} color={COLOUR.OFF_WHITE} />;
};
