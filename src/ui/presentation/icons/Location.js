import FontAwesome from 'react-native-vector-icons/FontAwesome';
import { COLOUR } from '~constants';

export const Location = () => {
  return <FontAwesome name="map-marker" size={100} color={COLOUR.WHITE} />;
};
